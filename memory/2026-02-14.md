# 2026-02-14

## Website-Prototyp / Markus
- Markus wollte einen sichtbaren Prototyp mit modernem Carousel (aktives Element vergrößert, Sticky-Nav unten am Carousel).
- Umsetzung in `fullstack-portfolio` wurde mehrfach angepasst; klare V2 mit horizontalem 1-Karten-Carousel erstellt.
- Wichtige Erwartung von Markus: lokale Dev-Umgebung über `npm run dev` auf `localhost:3333` (Nuxt-Projekt), so wie zuvor.

## Betriebs-/Umgebungs-Learnings
- In der Agent-Sandbox ist `npm` nicht verfügbar (`sh: 1: npm: not found`), daher kann der Agent den lokalen Dev-Server nicht selbst starten.
- Pfad-Mapping: Host-Pfad `/Users/markustratschitt/.openclaw/workspace` entspricht in der Sandbox effektiv `/workspace`-Kontext.

## Bugfix Nuxt (MDI 404)
- Fehler im Browser: `GET http://localhost:3333/_nuxt/@mdi/font/css/materialdesignicons.css 404`.
- Fix durchgeführt in `fullstack-portfolio-nuxt/nuxt.config.ts`:
  - `@mdi/font` aus `css: [...]` entfernt (kein `_nuxt`-Assetpfad mehr).
  - MDI stattdessen über CDN-`link` in `app.head.link` eingebunden.
- Nächster Schritt für Markus lokal: Dev-Server neu starten und Hard Reload.
