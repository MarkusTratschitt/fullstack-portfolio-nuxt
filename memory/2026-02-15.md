# 2026-02-15

## OpenClaw Multi-Agent Setup
- Markus wollte mehrere Agenten in OpenClaw nutzen.
- Teamrouting wurde erfolgreich getestet (`TEAMROUTING_OK | Rolle: Subagent`).
- Relevante Hinweise:
  - Gateway läuft und antwortet (`RPC probe: ok`, loopback local).
  - `openclaw doctor --repair` bleibt optional empfohlen wegen nvm-Node-Service-Warnung.

## Team-Konfiguration / Playbook
- `agent-team-nuxt.json` wurde auf ein sauberes Rollen-Playbook erweitert:
  - Orchestrator mit klarer Triage-/Delegationslogik
  - Spezialrollen: `frontend-nuxt`, `ui-vuetify`, `qa-review`
  - Routing-Regeln, Execution-Policy, Definition of Done in `notes.rolePlaybook`
- Aktivierung lokal durch Markus nötig:
  - `cp .../agent-team-nuxt.json ~/.openclaw/agent-team.json`
  - `openclaw gateway restart`

## Website-Stand (Nuxt Carousel Projekt)
- Viele Iterationen umgesetzt in `fullstack-portfolio-nuxt`:
  - 3D-Ring-Carousel mit Sticky-Nav
  - DE/EN Inhalte, SEO-Head, Mode- und Language-Switch
  - Kontaktformular mit mailto-Prefill
  - Social-Buttons, A11y-Verbesserungen, visuelle Feintuning-Wünsche
- Rechtliche Links als separate Seiten in neuem Tab:
  - `/datenschutz`
  - `/impressum`

## Nuxt-Team Governance & Workshop (neu)
- Markus hat verbindliche Teamregeln festgelegt:
  - Clean Code only
  - Strikte Typisierung ausnahmslos
  - Best-Practice-only
  - Keine Änderungen an bestehender Codebasis ohne Rückfrage + explizite Bestätigung
- Diese Regeln wurden in der lokalen Teamkonfiguration umgesetzt (`agent-team-nuxt.json`) und als Dokumente abgelegt:
  - `NUXT_TEAM_PLAYBOOK.md`
  - `NUXT_TEAM_PLAYBOOK_V1.md`
  - `NUXT_TEAM_WORKSHOP.md`
  - mehrere operative Docs unter `docs/` (Architektur, i18n, UI, Performance, Testing, CI, DoD, 2-Wochen-Plan)
- Zusätzlich erstellt: `.github/PULL_REQUEST_TEMPLATE.md` mit Approval-Gate und Qualitätschecks.

## GitHub-Repo & Branch-Protection Status
- Repo wurde auf GitHub veröffentlicht: `MarkusTratschitt/fullstack-portfolio-nuxt` (public, weil private Branch Protection auf Free nicht verfügbar war).
- Branch Protection auf `master` erfolgreich aktiv:
  - required checks strict
  - required contexts: `build`, `lint`, `typecheck`, `unit-tests`, `e2e-smoke`
  - 1 required review
  - stale review dismissal aktiv
  - conversation resolution aktiv
  - admins enforced
- Smoke-Test durchgeführt: PR-Status zeigte `BLOCKED`/`REVIEW_REQUIRED` (Policy greift).

## CI-Workflow Anpassungen im Repo
- `fullstack-portfolio-nuxt/.github/workflows/ci.yml` erweitert auf Jobs:
  - `lint`, `typecheck`, `unit-tests`, `build`, `e2e-smoke`
- `fullstack-portfolio-nuxt/package.json` ergänzt um Scripts für diese Checks.
- Aktueller Stand: `typecheck` ist echt, `lint/unit-tests/e2e-smoke` noch Placeholder-Kommandos bis ESLint/Vitest/Playwright vollständig eingerichtet sind.
